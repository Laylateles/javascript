function soma(n1=0, n2=0) {// o '= 0' é para q se n for digitado um número para algum dos parâmetros ele vai considerar o número 0
    return n1 + n2
}

var som = soma(3,7)
console.log(`A soma dos números é ${som}`)